<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog</title>
</head>
<body>
    <% if (user) { %>
        <p>Welcome <%= user.username %> to our Blog List</p>
        <p>Below is a list of all available blogs</p>
        <ul>
        <% blogs.forEach(function(blog) { %>
        <li><strong><%= blog.title %></strong>: <%= blog.content%></li>
        <% }); %>
        </ul>
        <br />
        <form action="/logout" method="post">
          <button type="submit">Logout</button>
        </form>

    <% } else { %>
        <p>Please <a href="/login">login</a> to view the Blog List</p>
    <% } %>
</body>
</html>